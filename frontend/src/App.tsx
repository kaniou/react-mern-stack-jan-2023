import React from "react";

type Props = {};

export default function App({}: Props) {
  // Typescript
  const tmp1 = "lek";

  // JSX(TSX)
  return (
    <div>
      <button>{tmp1}</button>
    </div>
  );
}
